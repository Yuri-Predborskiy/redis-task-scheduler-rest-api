{
  "name": "redis-server",
  "version": "1.0.0",
  "description": "A node.js-express RESTful API that has one route to schedule tasks. Task is saved to redis, which is used as task queue.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yuri Predborski",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "redis": "^2.8.0"
  }
}
